# Memory Atlas 0.2.1 packaged-build verification

**Verification date:** 2026-09-07

**Status:** Automated release gate passed; installed-app interaction checks outstanding

**Source commit:** `acc9eeeeadda516a05a2146c8a99480b2065879a` (clean working tree)

**Host:** darwin 25.6.0, arm64

**Toolchain:** Node 24.20.0, npm 11.19.0

## Automated source and packaging gates

- Clean dependency installation from `package-lock.json`: passed.
- Production dependency audit: passed with 0 total (0 info, 0 low, 0 moderate, 0 high, 0 critical).
- Complete dependency audit: passed the accepted 2026-09-07 baseline with 27 total (0 info, 3 low, 0 moderate, 23 high, 1 critical).
- Svelte and TypeScript checks: passed.
- Automated tests: passed.
- Vite production build: passed.
- Electron Forge packaging and DMG creation: passed for `darwin/arm64`.
- Packaged ASAR content inspection: passed.
- Temporary and DMG-embedded application signatures: passed deep/strict verification.
- DMG checksum verification: passed.

The complete dependency audit is a comparison with the reviewed residual-risk
baseline in `docs/operations/dependency-security.md`. The release script never changes
dependencies or runs `npm audit fix`. A new advisory or a count above the
baseline stops the release for separate review.

## Artifact identity

| Property | Verified value |
| --- | --- |
| Application version | `0.2.1` |
| Architecture | `arm64` |
| Artifact | `out/make/Memory Atlas-0.2.1-arm64.dmg` |
| Size | 122,734,871 bytes |
| SHA-256 | `861ee4f6adf3a7e2cce8e56cfd2c4412c78666ea280abc27ff8fc0a4acc50d14` |
| Bundle identifier | `io.github.chrwittm.memoryatlas` |
| App CDHash | `9d3778c7315397bdace50395c709f26c22b44d46` |
| Signing | Ad hoc; deep/strict verification passed |
| Team identifier | not set |
| Notarization | Not notarized |

Detailed command logs and the machine-readable report were generated under
`out/release/0.2.1-arm64-20260907T163623Z`. That directory is ignored by Git.

## Manual verification still required

Install the exact DMG and complete the installed-app smoke test in
`docs/operations/macos-packaging.md`. Do not publish or tag this build until
the intended release workflow, release notes, licensing state, and manual checks
have been reviewed.

## Installation follow-up — 2026-09-08

The exact DMG application was installed in `/Applications/Memory Atlas.app`,
and the installed application passed deep/strict signature verification. The
previous application was preserved outside the repository. Source synthetic
JPEGs remain byte-identical to the pre-implementation history.

The startup-error dialog initially observed by native automation belonged to a
stale process launched on 2026-09-04 under `com.memoryatlas.app`. It is not
evidence that this 0.2.1 artifact failed startup. After closing that old process,
the tool listed the new `io.github.chrwittm.memoryatlas` application as running
but rejected attempts to attach to its bundle identifier. Installed fullscreen
and the remaining manual checklist therefore need direct user verification.
No tag or public DMG release was created from incomplete interaction evidence.

## Owner interaction result

On 2026-09-08 the owner explicitly confirmed the entry screen, JPEG folder
loading, fullscreen entry and exit with `F` in photo-only and split-map layouts,
and Escape priority all pass. This closes MA-BUG-002 for this candidate.

A subsequent dependency-license inspection found missing runtime notices and MPL
source-availability materials. This artifact will not be published: a final
packaging-only rebuild must include those materials and retain separate exact
source/checksum evidence. The checksum above remains the identity of this
unpublished candidate.
