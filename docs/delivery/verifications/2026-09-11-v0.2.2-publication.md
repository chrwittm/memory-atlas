# Memory Atlas 0.2.2 packaged-build verification

**Verification date:** 2026-09-11

**Status:** Published tester prerelease; release gate, installed checks, and public checksum verified

**Source commit:** `430c348fa12b0fe6d180b3e40f518daeac25930d` (clean working tree)

**Host:** darwin 25.6.0, arm64

**Toolchain:** Node 24.20.0, npm 11.19.0

## Automated source and packaging gates

- Clean dependency installation from `package-lock.json`: passed.
- Production dependency audit: passed with 0 total (0 info, 0 low, 0 moderate, 0 high, 0 critical).
- Complete dependency audit: passed the accepted 2026-09-11 baseline with 27 total (0 info, 3 low, 0 moderate, 23 high, 1 critical).
- Svelte and TypeScript checks: passed.
- Automated tests: passed.
- Vite production build: passed.
- Electron Forge packaging and DMG creation: passed for `darwin/arm64`.
- Packaged ASAR content inspection: passed.
- Temporary and DMG-embedded application signatures: passed deep/strict verification.
- DMG checksum verification: passed.

The complete dependency audit is a comparison with the reviewed residual-risk
baseline in `docs/operations/dependency-security.md`. The release script never changes
dependencies or runs `npm audit fix`. A new advisory or a count above the
baseline stops the release for separate review.

## Artifact identity

| Property | Verified value |
| --- | --- |
| Application version | `0.2.2` |
| Architecture | `arm64` |
| Artifact | `out/make/Memory Atlas-0.2.2-arm64.dmg` |
| Size | 122,940,936 bytes |
| SHA-256 | `56089a8ec30fb015423bb02ab30b3a09b1ecbd4af71fef79f2b0b6fd88f24f42` |
| Bundle identifier | `io.github.chrwittm.memoryatlas` |
| App CDHash | `08be5118ed2f6f70a84643aa584629e676273ef8` |
| Signing | Ad hoc; deep/strict verification passed |
| Team identifier | not set |
| Notarization | Not notarized |

Detailed command logs and the machine-readable report were generated under
`out/release/0.2.2-arm64-20260911T061007Z`. That directory is ignored by Git.

## Installed-app verification

Installed the exact DMG into Applications using `ditto --norsrc`, then repeated
deep/strict signature verification. Native computer use verified the entry
screen, synthetic JPEG folder loading and natural ordering, caption and capture
time, information toggle, rendered map tiles and attribution, no-GPS placeholder,
map close/reopen, fullscreen entry and exit in both layouts, Escape priority,
double-click zoom, drag pan, Z fitted-view restoration, malformed metadata,
unreadable-photo isolation, and quit/relaunch back to the entry screen.
The host's German keyboard layout required the automation's physical Y key for
Z; the app displayed the expected fitted-view feedback.

The four synthetic test-file checksums remain unchanged. No private collection
was imported for these replacement checks. The earlier owner report covers
unchanged offline photo behavior; network disconnection was not repeated in
this replacement smoke test. Known offline tile recovery issue MA-BUG-001
remains documented and is not claimed fixed.

## Security and source evidence

MapLibre is pinned to upstream 6.4.1, fixing GHSA-jrc7-96c5-q579; Vitest is
4.1.11. The real attribution-control regression rejects adjacent unsafe HTML
attributes while preserving a safe credit link. All 64 tests across 16 files
pass. The packaged MapLibre worker is local and required by the ASAR gate.
[Exact-source GitHub CI](https://github.com/chrwittm/memory-atlas/actions/runs/34568838945)
passed. GitHub's refreshed default-branch Dependabot report contains no open
runtime alerts; 12 development alert records remain. This is distinct from
npm's 27 propagated affected package entries.

The 0.2.1 draft, tag and checksum remain historical. No published artifact was
silently replaced with different bytes. Gate C remains deferred: this artifact
is explicitly an unnotarized Apple silicon tester prerelease.

## Public download verification

The annotated `v0.2.2` tag points to the exact source commit above.
[The release](https://github.com/chrwittm/memory-atlas/releases/tag/v0.2.2)
contains only `Memory.Atlas-0.2.2-arm64.dmg`. An independent unauthenticated
HTTPS download on September 11 matched the original SHA-256 exactly:

```text
56089a8ec30fb015423bb02ab30b3a09b1ecbd4af71fef79f2b0b6fd88f24f42
```

Gates A and B are complete. Later documentation commits do not alter the tagged
artifact. Documentation validation checked 39 Markdown files with zero local
link-destination problems.
