# Memory Atlas 0.2.1 packaged-build verification

**Verification date:** 2026-09-08

**Status:** Automated release gate passed; installed-app interaction checks outstanding

**Source commit:** `074ac8b485d666da074fd4cbb4e6dcddfffc9334` (clean working tree)

**Host:** darwin 25.6.0, arm64

**Toolchain:** Node 24.20.0, npm 11.19.0

## Automated source and packaging gates

- Clean dependency installation from `package-lock.json`: passed.
- Production dependency audit: passed with 0 total (0 info, 0 low, 0 moderate, 0 high, 0 critical).
- Complete dependency audit: passed the accepted 2026-09-07 baseline with 27 total (0 info, 3 low, 0 moderate, 23 high, 1 critical).
- Svelte and TypeScript checks: passed.
- Automated tests: passed.
- Vite production build: passed.
- Electron Forge packaging and DMG creation: passed for `darwin/arm64`.
- Packaged ASAR content inspection: passed.
- Temporary and DMG-embedded application signatures: passed deep/strict verification.
- DMG checksum verification: passed.

The complete dependency audit is a comparison with the reviewed residual-risk
baseline in `docs/operations/dependency-security.md`. The release script never changes
dependencies or runs `npm audit fix`. A new advisory or a count above the
baseline stops the release for separate review.

## Artifact identity

| Property | Verified value |
| --- | --- |
| Application version | `0.2.1` |
| Architecture | `arm64` |
| Artifact | `out/make/Memory Atlas-0.2.1-arm64.dmg` |
| Size | 122,851,622 bytes |
| SHA-256 | `2dfa3d5aa90085f66530944b38d9117c994e73694ca3737a00ad1529a78dfc2c` |
| Bundle identifier | `io.github.chrwittm.memoryatlas` |
| App CDHash | `81a34ef5099c23352e38f2678dbc9f684e3eb8b4` |
| Signing | Ad hoc; deep/strict verification passed |
| Team identifier | not set |
| Notarization | Not notarized |

Detailed command logs and the machine-readable report were generated under
`out/release/0.2.1-arm64-20260908T044625Z`. That directory is ignored by Git.

## Manual verification still required

Install the exact DMG and complete the installed-app smoke test in
`docs/operations/macos-packaging.md`. Do not publish or tag this build until
the intended release workflow, release notes, licensing state, and manual checks
have been reviewed.

## Final candidate installation and publication status

The final candidate was installed in `/Applications/Memory Atlas.app` and passed
deep/strict signature verification. Its eight runtime JavaScript, CSS, and HTML
files match the earlier owner-tested candidate byte for byte; the rebuild adds
third-party notices and MPL source materials. The generated legal resources and
packaged copies passed recursive file/hash comparison.

The owner confirmed entry-screen display, JPEG folder loading, `F` entry and exit
in photo-only and split-map views, and Escape priority on the earlier candidate.
See [that candidate's record](2026-09-08-v0.2.1-candidate.md). The remaining
installed-app checks (zoom/pan, information toggle, map states, unreadable-file
recovery, offline viewing, relaunch, and icon appearance) await owner results.
Native automation cannot attach to the replacement bundle identity, so these
checks are not represented as automated passes. Synthetic smoke-test JPEG
checksums were checked again on 2026-09-08 and remain unchanged.

[Source CI](https://github.com/chrwittm/memory-atlas/actions/runs/34188199093)
passed for the exact candidate commit. The public README image was visually
checked at desktop and 390-pixel viewport widths. Release notes are prepared;
no public tester release or public-download checksum verification is claimed.
