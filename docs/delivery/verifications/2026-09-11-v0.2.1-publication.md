# Memory Atlas 0.2.1 publication verification

**Date:** 2026-09-11
**Status:** Gates A and B complete; public download verified

The owner reported successful local testing of the installed 0.2.1 app and
cleared the remaining checklist. Explicit observations included startup, the
custom icon, zooming, panning, restoring zoom levels, captions and their toggle,
and correct map behavior. Fullscreen entry/exit in both layouts and Escape
priority were previously confirmed. These are owner observations, not automated
UI results.

The [final candidate record](2026-09-08-v0.2.1-final-candidate.md) preserves the
2026-09-08 automated gate and exact binary identity. The annotated `v0.2.1` tag
points to `074ac8b485d666da074fd4cbb4e6dcddfffc9334`, the clean source used to
build that artifact. Later documentation commits do not change the binary.
Synthetic smoke-test source checksums were verified unchanged on 2026-09-11.

The release is Apple silicon (`arm64`), ad-hoc signed and unnotarized; Developer
ID signing and notarization (Gate C) remain deferred by the accepted decision.
The release gate's September 8 audit passed with zero production findings and
27 accepted build-tool findings; this is dated evidence, not a fresh audit.

## Local duplicate application

Spotlight's generic-icon result came from the generated repository output
`out/Memory Atlas-darwin-arm64/Memory Atlas.app`. Its Info.plist identified
version 0.1.0 and `com.memoryatlas.app`; the installed Applications copy is
0.2.1 with `io.github.chrwittm.memoryatlas`. The obsolete generated app was
removed. Its launch-registration cleanup returned a scan error, so immediate
Spotlight cache refresh is not claimed. The release DMG remains intact.

## Public release and download

[Memory Atlas 0.2.1](https://github.com/chrwittm/memory-atlas/releases/tag/v0.2.1)
is published with `draft: false` and `prerelease: true`. Its only asset is
`Memory.Atlas-0.2.1-arm64.dmg`, 122,851,622 bytes. GitHub's asset digest and an
independent unauthenticated public download both match the original DMG:

```text
SHA-256: 2dfa3d5aa90085f66530944b38d9117c994e73694ca3737a00ad1529a78dfc2c
```

The downloaded bytes therefore match the artifact whose DMG integrity and
strict application signature passed the release gate. The exact-source
[GitHub CI run](https://github.com/chrwittm/memory-atlas/actions/runs/34188199093)
passed. Documentation link validation checked 38 Markdown files with zero
local destination problems. No private photo data or historical backup refs
were published.
